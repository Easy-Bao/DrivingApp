// This file is automatically generated, so please do not edit it.
// @generated by `flutter_rust_bridge`@ 2.12.0.

// ignore_for_file: invalid_use_of_internal_member, unused_import, unnecessary_import

import '../frb_generated.dart';
import 'package:flutter_rust_bridge/flutter_rust_bridge_for_generated.dart';

/// Configuration for fare calculation.
/// Pass custom values for different vehicle types or surge periods.
class FareConfig {
  /// Fixed starting cost (e.g., ₱20.00).
  final double baseFare;

  /// Cost per kilometer driven (e.g., ₱10.00).
  final double perKmRate;

  /// Cost per minute of travel time (e.g., ₱1.50).
  final double perMinuteRate;

  /// Multiplier applied during high-demand periods (1.0 = no surge).
  final double surgeMultiplier;

  /// Floor price — fare never goes below this (e.g., ₱25.00).
  final double minimumFare;

  const FareConfig({
    required this.baseFare,
    required this.perKmRate,
    required this.perMinuteRate,
    required this.surgeMultiplier,
    required this.minimumFare,
  });

  @override
  int get hashCode =>
      baseFare.hashCode ^
      perKmRate.hashCode ^
      perMinuteRate.hashCode ^
      surgeMultiplier.hashCode ^
      minimumFare.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is FareConfig &&
          runtimeType == other.runtimeType &&
          baseFare == other.baseFare &&
          perKmRate == other.perKmRate &&
          perMinuteRate == other.perMinuteRate &&
          surgeMultiplier == other.surgeMultiplier &&
          minimumFare == other.minimumFare;
}

/// Itemized result of a fare calculation.
/// All monetary values are in Philippine Peso (₱).
class FareResult {
  final double baseFare;
  final double distanceCharge;
  final double timeCharge;
  final double surgeCharge;
  final double totalFare;

  const FareResult({
    required this.baseFare,
    required this.distanceCharge,
    required this.timeCharge,
    required this.surgeCharge,
    required this.totalFare,
  });

  @override
  int get hashCode =>
      baseFare.hashCode ^
      distanceCharge.hashCode ^
      timeCharge.hashCode ^
      surgeCharge.hashCode ^
      totalFare.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is FareResult &&
          runtimeType == other.runtimeType &&
          baseFare == other.baseFare &&
          distanceCharge == other.distanceCharge &&
          timeCharge == other.timeCharge &&
          surgeCharge == other.surgeCharge &&
          totalFare == other.totalFare;
}

/// A single cell in the surge demand heatmap.
/// Rendered as a colored overlay on the driver's map.
class HeatmapCell {
  final double lat;
  final double lng;

  /// Surge intensity as a multiplier:
  /// 1.0 = normal, 1.5 = 50% surge, 2.0 = 100% surge.
  final double intensity;

  const HeatmapCell({
    required this.lat,
    required this.lng,
    required this.intensity,
  });

  @override
  int get hashCode => lat.hashCode ^ lng.hashCode ^ intensity.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is HeatmapCell &&
          runtimeType == other.runtimeType &&
          lat == other.lat &&
          lng == other.lng &&
          intensity == other.intensity;
}
