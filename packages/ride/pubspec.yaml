name: ride
description: Shared ride domain entities, fare contracts, and workflow failures.
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.11.3

dependencies:
  equatable: ^2.0.7
  foundation:
    path: ../foundation

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
