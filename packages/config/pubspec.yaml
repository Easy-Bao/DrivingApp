name: config
description: "Shared environment configuration package for DriveApp services and clients."
version: 0.0.1
homepage:

environment:
  sdk: ^3.11.3
  flutter: ">=1.17.0"

publish_to: 'none'

dependencies:
  flutter:
    sdk: flutter
  flutter_dotenv: ^6.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
