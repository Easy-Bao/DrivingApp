name: shared_core
description: Shared domain models, transport contracts, errors, fare DTOs, and realtime clients.
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.11.3

dependencies:
  dio: ^5.10.0
  equatable: ^2.0.7
  foundation:
    path: ../foundation
  fpdart: ^1.2.0
  json_annotation: ^4.9.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
