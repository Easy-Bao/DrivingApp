name: shared_core
description: Shared domain models, HTTP clients, map services, fare contracts, and realtime DTOs.
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.11.3
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  dio: ^5.10.0
  equatable: ^2.0.7
  fpdart: ^1.2.0
  flutter_polyline_points: ^2.1.0
  geolocator: ^13.0.2
  json_annotation: ^4.9.0
  map_launcher: ^3.4.0
  mapbox_maps_flutter: ^2.23.0
  permission_handler: ^11.4.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
