name: location_service
description: "Map native and geolocation services using Mapbox and Dio."
version: 0.0.1
homepage:

environment:
  sdk: ^3.12.2
  flutter: ">=1.17.0"

publish_to: 'none'

dependencies:
  flutter:
    sdk: flutter
  geolocator: ^13.0.2
  mapbox_maps_flutter: ^2.23.0
  map_launcher: ^3.4.0
  flutter_polyline_points: ^2.1.0
  permission_handler: ^11.4.0
  dio: ^5.10.0
  core_models:
    path: ../core_models

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
