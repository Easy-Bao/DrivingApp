import 'package:design_system/src/widgets/easy_ride_layout.dart';

export 'easy_ride_layout.dart';

/// Compatibility alias for clients that still use the former generic name.
@Deprecated('Use EasyRidePageHeader instead.')
typedef AppPageHeader = EasyRidePageHeader;

/// Compatibility alias for clients that still use the former generic name.
@Deprecated('Use EasyRidePageFrame instead.')
typedef AppPageFrame = EasyRidePageFrame;

/// Compatibility alias for clients that still use the former generic name.
@Deprecated('Use EasyRideSurfaceCard instead.')
typedef AppSurfaceCard = EasyRideSurfaceCard;

/// Compatibility alias for clients that still use the former generic name.
@Deprecated('Use EasyRideNavigationRail instead.')
typedef AppNavigationRail = EasyRideNavigationRail;
