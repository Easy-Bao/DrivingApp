name: chat_service
description: "Shared WebSocket Chat Service for Rider and Driver apps."
version: 0.0.1
publish_to: 'none'

environment:
  sdk: '>=3.12.2 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  fpdart: ^1.2.0
  freezed_annotation: ^3.0.0
  json_annotation: ^4.9.0
  core_models:
    path: ../core_models


dev_dependencies:
  build_runner: ^2.4.15
  freezed: ^3.0.0
  json_serializable: ^6.9.4
