name: maps
description: Shared map, location, search, routing, and geospatial capabilities.
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.13.0
  flutter: '>=3.47.2'

dependencies:
  flutter:
    sdk: flutter
  dio: ^5.10.0
  equatable: ^2.0.7
  foundation:
    path: ../foundation
  fpdart: ^1.2.0
  geolocator: ^14.0.3
  mapbox_maps_flutter: ^2.23.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
