name: fixtures
description: Standalone package for testing fixtures and mock data.
version: 1.0.0
publish_to: 'none'

environment:
  sdk: ^3.12.2

dependencies:
  core_models:
    path: ../core_models

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
