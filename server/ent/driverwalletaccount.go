// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/Easy-Bao/DrivingApp/server/ent/driverwalletaccount"
)

// DriverWalletAccount is the model entity for the DriverWalletAccount schema.
type DriverWalletAccount struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// DriverID holds the value of the "driver_id" field.
	DriverID int `json:"driver_id,omitempty"`
	// BalanceCentavos holds the value of the "balance_centavos" field.
	BalanceCentavos int64 `json:"balance_centavos,omitempty"`
	// Version holds the value of the "version" field.
	Version int64 `json:"version,omitempty"`
	// UpdatedAt holds the value of the "updated_at" field.
	UpdatedAt    time.Time `json:"updated_at,omitempty"`
	selectValues sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*DriverWalletAccount) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case driverwalletaccount.FieldID, driverwalletaccount.FieldDriverID, driverwalletaccount.FieldBalanceCentavos, driverwalletaccount.FieldVersion:
			values[i] = new(sql.NullInt64)
		case driverwalletaccount.FieldUpdatedAt:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the DriverWalletAccount fields.
func (_m *DriverWalletAccount) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case driverwalletaccount.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			_m.ID = int(value.Int64)
		case driverwalletaccount.FieldDriverID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field driver_id", values[i])
			} else if value.Valid {
				_m.DriverID = int(value.Int64)
			}
		case driverwalletaccount.FieldBalanceCentavos:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field balance_centavos", values[i])
			} else if value.Valid {
				_m.BalanceCentavos = value.Int64
			}
		case driverwalletaccount.FieldVersion:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field version", values[i])
			} else if value.Valid {
				_m.Version = value.Int64
			}
		case driverwalletaccount.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				_m.UpdatedAt = value.Time
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the DriverWalletAccount.
// This includes values selected through modifiers, order, etc.
func (_m *DriverWalletAccount) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// Update returns a builder for updating this DriverWalletAccount.
// Note that you need to call DriverWalletAccount.Unwrap() before calling this method if this DriverWalletAccount
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *DriverWalletAccount) Update() *DriverWalletAccountUpdateOne {
	return NewDriverWalletAccountClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the DriverWalletAccount entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *DriverWalletAccount) Unwrap() *DriverWalletAccount {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: DriverWalletAccount is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *DriverWalletAccount) String() string {
	var builder strings.Builder
	builder.WriteString("DriverWalletAccount(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("driver_id=")
	builder.WriteString(fmt.Sprintf("%v", _m.DriverID))
	builder.WriteString(", ")
	builder.WriteString("balance_centavos=")
	builder.WriteString(fmt.Sprintf("%v", _m.BalanceCentavos))
	builder.WriteString(", ")
	builder.WriteString("version=")
	builder.WriteString(fmt.Sprintf("%v", _m.Version))
	builder.WriteString(", ")
	builder.WriteString("updated_at=")
	builder.WriteString(_m.UpdatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// DriverWalletAccounts is a parsable slice of DriverWalletAccount.
type DriverWalletAccounts []*DriverWalletAccount
