// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/Easy-Bao/DrivingApp/server/ent/bid"
)

// Bid is the model entity for the Bid schema.
type Bid struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// RideID holds the value of the "ride_id" field.
	RideID int `json:"ride_id,omitempty"`
	// DriverID holds the value of the "driver_id" field.
	DriverID int `json:"driver_id,omitempty"`
	// OfferedFareCentavos holds the value of the "offered_fare_centavos" field.
	OfferedFareCentavos int64 `json:"offered_fare_centavos,omitempty"`
	// Status holds the value of the "status" field.
	Status       string `json:"status,omitempty"`
	selectValues sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*Bid) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case bid.FieldID, bid.FieldRideID, bid.FieldDriverID, bid.FieldOfferedFareCentavos:
			values[i] = new(sql.NullInt64)
		case bid.FieldStatus:
			values[i] = new(sql.NullString)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the Bid fields.
func (_m *Bid) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case bid.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			_m.ID = int(value.Int64)
		case bid.FieldRideID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field ride_id", values[i])
			} else if value.Valid {
				_m.RideID = int(value.Int64)
			}
		case bid.FieldDriverID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field driver_id", values[i])
			} else if value.Valid {
				_m.DriverID = int(value.Int64)
			}
		case bid.FieldOfferedFareCentavos:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field offered_fare_centavos", values[i])
			} else if value.Valid {
				_m.OfferedFareCentavos = value.Int64
			}
		case bid.FieldStatus:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field status", values[i])
			} else if value.Valid {
				_m.Status = value.String
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the Bid.
// This includes values selected through modifiers, order, etc.
func (_m *Bid) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// Update returns a builder for updating this Bid.
// Note that you need to call Bid.Unwrap() before calling this method if this Bid
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *Bid) Update() *BidUpdateOne {
	return NewBidClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the Bid entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *Bid) Unwrap() *Bid {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: Bid is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *Bid) String() string {
	var builder strings.Builder
	builder.WriteString("Bid(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("ride_id=")
	builder.WriteString(fmt.Sprintf("%v", _m.RideID))
	builder.WriteString(", ")
	builder.WriteString("driver_id=")
	builder.WriteString(fmt.Sprintf("%v", _m.DriverID))
	builder.WriteString(", ")
	builder.WriteString("offered_fare_centavos=")
	builder.WriteString(fmt.Sprintf("%v", _m.OfferedFareCentavos))
	builder.WriteString(", ")
	builder.WriteString("status=")
	builder.WriteString(_m.Status)
	builder.WriteByte(')')
	return builder.String()
}

// Bids is a parsable slice of Bid.
type Bids []*Bid
